name: Crawler Health Check

on:
  schedule:
    - cron: "30 19 * * *"   # 매일 KST 04:30 — crawl.yml 이후 실행되도록
  workflow_dispatch:

jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
      - run: npm ci
      # 실패 시 non-zero exit → GitHub가 자동으로 저장소 소유자에게 이메일 알림 발송.
      # (GitHub 개인 설정 → Notifications → "Workflows" 켜져있어야 함. 기본값 ON.)
      - name: Check crawler health
        run: npx tsx scripts/alerts/check-crawlers.ts

      # detected_changes.pending 누적 감지 — 법령 개정 신호가 방치되면 경고.
      # if: always() 로 crawler-health 실패해도 이 단계도 돌림 (독립 판단).
      - name: Check detected changes queue
        if: always()
        run: npx tsx scripts/alerts/check-detected-changes.ts
